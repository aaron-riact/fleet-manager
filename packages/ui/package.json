{
  "name": "@fleet-manager/ui",
  "version": "0.1.0",
  "description": "Vite React ops UI: SRP login, map SVG with nodes/edges/locks (phase 4)",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo 'ui: stub (phase 4)'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
