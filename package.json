{
  "name": "fleet-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Modular VDA5050 fleet manager — Node/Bun server and serverless browser demo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter './packages/*' build",
    "test": "bun test packages/",
    "typecheck": "bun run --filter './packages/*' typecheck"
  },
  "devDependencies": {
    "bun-types": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
